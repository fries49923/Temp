<Window x:Class="ToolKitMvvmTest01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToolKitMvvmTest01"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="450"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <Thickness x:Key="Margin.Top">0,10,0,0</Thickness>

    </Window.Resources>

    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>

        <StackPanel Margin="20">

            <!--Name-->
            <TextBlock Text="Name:"/>
            <TextBox Text="{Binding Path=Name,
                                    UpdateSourceTrigger=PropertyChanged}"/>

            <!--Phone-->
            <TextBlock Margin="{StaticResource Margin.Top}"
                       Text="Phone:"/>
            <TextBox Text="{Binding Path=Phone,
                                    UpdateSourceTrigger=PropertyChanged}"/>

            <Button Margin="{StaticResource Margin.Top}"
                    Content="Show Info"
                    Command="{Binding Path=ShowInfoCommand}"/>

            <!--Note-->
            <TextBlock Margin="{StaticResource Margin.Top}"
                       Text="Note:"/>
            <TextBox x:Name="tbNote"/>

            <Button Margin="{StaticResource Margin.Top}"
                    Content="Show Note"
                    Command="{Binding Path=ShowNoteCommand}"
                    CommandParameter="{Binding ElementName=tbNote, Path=Text}"/>

        </StackPanel>

    </Grid>

</Window>
